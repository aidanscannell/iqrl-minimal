total_timesteps: 5000000

agent:
  latent_dim: 64
  reset_params_freq: 200000  # reset params after this many param updates
  exploration_noise_start: 1.0
  exploration_noise_end: 0.1
  exploration_noise_num_steps: 150  # number of episodes do decay noise

hydra:
  launcher:
    timeout_min: 720 # 12 hours

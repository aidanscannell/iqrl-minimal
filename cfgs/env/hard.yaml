# train_episode: 5000
total_timesteps: 5000000
# std_schedule: linear(1.0, 0.2, 500)
agent:
  latent_dim: 128
  reset_params_freq: 100000  # reset params after this many param updates
  exploration_noise_start: 1.0
  exploration_noise_end: 0.2
  exploration_noise_num_steps: 500  # number of episodes do decay noise

hydra:
  launcher:
    timeout_min: 1440 # 24 hours
